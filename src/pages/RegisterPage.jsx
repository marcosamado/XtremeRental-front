import { RegisterForm } from '../components/register/RegisterForm';

const RegisterPage = () => {
    return (
        <div className="flex flex-col py-24 bg-gradient-to-b from-red-100 to-sky-100">
            <RegisterForm />
        </div>
    );
};

export default RegisterPage;
